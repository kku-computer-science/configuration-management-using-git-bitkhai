> **📝 AI Declaration:** Documentation: ใช้ AI ช่วยเรียบเรียงภาษาในเอกสาร Markdown ให้มีความเป็นทางการและอ่านเข้าใจง่าย

# 🤝 Team Workflow & Project Structure Documentation

เอกสารฉบับนี้รวบรวมข้อมูลสมาชิกในทีม โครงสร้างของโปรเจกต์ และกระบวนการทำงานร่วมกัน (Workflow) เพื่อให้การพัฒนาซอฟต์แวร์เป็นไปอย่างมีระบบ ตรวจสอบได้ และสอดคล้องกับหลักการ Modular Design

## 🎥 สมาชิกในทีมและการแบ่งหน้าที่ (Team Members & Roles)

ทีมได้แบ่งหน้าที่ความรับผิดชอบตามไฟล์งาน โดยสมาชิกแต่ละคนรับผิดชอบโมดูลของตนเองอย่างชัดเจน:

| ชื่อ-นามสกุล | รหัสนักศึกษา | หน้าที่รับผิดชอบ (Role) | ไฟล์ที่รับผิดชอบ |
|---|---|---|---|
| **Kongphop Malimas** | 663380008-7 | **Project Manager & Integration**<br>ดูแลภาพรวม, รับ Input/Output, เชื่อมต่อโมดูล | `Project/main.py`<br>`AboutMyTeam/Kongphop_0087.md` |
| **Kantee Khaekham** | 663380198-6 | **Algorithm Developer**<br>พัฒนาส่วนประมวลผล Bubble Sort | `Project/bubblesort.py`<br>`AboutMyTeam/Kantee_1986.md` |
| **Khanet Buasod** | 663380203-9 | **Algorithm Developer**<br>พัฒนาส่วนประมวลผล Quick Sort | `Project/quicksort.py`<br>`AboutMyTeam/Khanet_2039.md` |

## 🔄 กระบวนการทำงานร่วมกัน (Workflow)

ทีมใช้กระบวนการพัฒนาแบบแยกส่วน (**Component-Based Development**) ควบคู่กับระบบ **Git Version Control** โดยมีขั้นตอนดังนี้:

### 1. การเตรียมเอกสาร (Documentation)
* สมาชิกทุกคนสร้างไฟล์ Markdown (`.md`) ส่วนตัวในโฟลเดอร์ `AboutMyTeam` เพื่อแนะนำตัว
* ใช้ไฟล์ `TeamWorkflow.md` เป็นศูนย์กลางในการอ้างอิงข้อตกลงร่วมกัน

### 2. การสร้าง Branch และเริ่มพัฒนางาน (Branching & Implementation)
เพื่อให้การทำงานไม่ทับซ้อนกัน ทีมปฏิบัติตามขั้นตอน Git Workflow ดังนี้:

* **2.1 สร้าง Branch ส่วนตัว (Branch Creation):**
  สมาชิกแต่ละคนสร้าง Branch จาก `main` และแยกย้ายไปทำงานตามหน้าที่:
  * Branch: `Kongphop_0087` ➔ พัฒนา `main.py` (Main System)
  * Branch: `Kantee_1986` ➔ พัฒนา `bubblesort.py` (Bubble Sort Algorithm)
  * Branch: `Khanet_2039` ➔ พัฒนา `quicksort.py` (Quick Sort Algorithm)

* **2.2 การเขียนโค้ด (Implementation):**
  * สมาชิกทำงานบน **Branch ของตนเองเท่านั้น**
  * เขียน Source Code ลงในโฟลเดอร์ `Project/` ตามไฟล์ที่รับผิดชอบ
  * สร้างไฟล์แนะนำตัว (`.md`) ของตนเองในโฟลเดอร์ `AboutMyTeam/`

### 3. มาตรฐานการเขียนโค้ด (Coding Standards)
ทีมตกลง **Interface** กลางเพื่อให้ Code สามารถทำงานร่วมกันได้เมื่อ Merge รวมกัน:
* **Input:** ฟังก์ชัน Sort ต้องรับค่าเป็น `list` ของตัวเลข (Integers)
* **Output:** ฟังก์ชันต้อง `return list` ที่เรียงลำดับแล้วกลับมา
* **Naming Convention:** ตั้งชื่อไฟล์และตัวแปรเป็นภาษาอังกฤษตัวพิมพ์เล็ก (**Lowercase**)

### 4. การรวมระบบ (Merge & Integration)
เมื่อโค้ดใน Branch ส่วนตัวเสร็จสมบูรณ์และผ่านการตรวจสอบเบื้องต้น:
1. สมาชิกทำการ **Pull** ข้อมูลล่าสุดจาก Main Branch เพื่ออัปเดต
2. แก้ไข **Conflict** (หากมี) ร่วมกับเพื่อนในทีม
3. **Merge** งานเข้าสู่ `main` Branch
4. **Kongphop** ทำการเชื่อมต่อ `main.py` เข้ากับ `bubblesort.py` และ `quicksort.py`


---
## 📂 โครงสร้างโปรเจกต์ (Project Structure)

โปรเจกต์ถูกจัดเก็บโดยแยกส่วนระหว่าง "เอกสารข้อมูลทีม" และ "Source Code" ดังแสดงในแผนผังด้านล่าง:

```text
📦 Root Directory
 ┣ 📂 .github
 ┃ ┗ 📄 .keep
 ┣ 📂 AboutMyTeam           # โฟลเดอร์เก็บข้อมูลสมาชิกและกระบวนการทำงาน
 ┃ ┣ 📄 Kantee_1986.md      # ข้อมูลแนะนำตัวของ Kantee
 ┃ ┣ 📄 Khanet_2039.md      # ข้อมูลแนะนำตัวของ Khanet
 ┃ ┣ 📄 Kongphop_0087.md    # ข้อมูลแนะนำตัวของ Kongphop
 ┃ ┗ 📄 TeamWorkflow.md     # อธิบาย Workflow ของทีม
 ┣ 📂 Project               # โฟลเดอร์เก็บ Source Code หลัก
 ┃ ┣ 📂 __pycache__
 ┃ ┣ 📄 bubblesort.py       # โมดูล Bubble Sort Algorithm
 ┃ ┣ 📄 main.py             # ไฟล์หลักสำหรับรันโปรแกรม
 ┃ ┗ 📄 quicksort.py        # โมดูล Quick Sort Algorithm
 ┗ 📄 README.md             # คู่มือการใช้งานโปรเจกต์เบื้องต้น
